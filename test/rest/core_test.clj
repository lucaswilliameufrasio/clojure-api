(ns rest.core-test
  (:require [clojure.test :refer [deftest testing is]]
            [rest.core :as core]))

(deftest a-test
  (testing "FIXME, I fail. (Whisper: now, you don't...)"
    (is (= 1 1))))
